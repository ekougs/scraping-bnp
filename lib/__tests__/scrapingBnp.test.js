const assert = require('assert');
// TODO const scrapingBnp = require('../index.js');

describe('scrapingBnp', () => {
  it('has a test', () => {
    assert(false, 'scrapingBnp should have a test');
  });
});
